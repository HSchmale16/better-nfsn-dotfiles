set nocompatible
" Set defaults for indentation
set expandtab
set tabstop=4
set shiftwidth=4

" Prevent the swap file from getting into the current directory
set directory=.,/home/tmp//
set backupdir=/home/tmp//

" Line numbering
set number
